# CDI StorageProfile for k3s local-path. cloneStrategy copy is host-assisted;
# do not apply Eureka's cdi-storageprofiles-csi-clone.yaml.
apiVersion: cdi.kubevirt.io/v1beta1
kind: StorageProfile
metadata:
  name: local-path
spec:
  cloneStrategy: copy
